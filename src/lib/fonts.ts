import { Inter } from 'next/font/google';

// Configure the Inter font with Next.js 15 compatible options
export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});

// Export the font for use in layout.tsx
export default inter;
